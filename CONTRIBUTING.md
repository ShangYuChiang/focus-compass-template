# 參與步步 Template 開發

謝謝你願意讓步步變得更好。歡迎透過 Issue 回報問題、提出建議，或送出 Pull Request。

## 開發流程

1. Fork 本倉庫並建立用途清楚的分支。
2. 執行 `npm ci` 安裝相依套件。
3. 修改功能並為重要行為新增或更新測試。
4. 送出變更前執行 `npm test` 與 `npm run build`。
5. 在 Pull Request 說明問題、修改內容與驗證方式。

## 隱私檢查

請勿提交真實任務、復盤內容、備份、資料庫、API 金鑰、`.env`、私人照片或本機絕對路徑。若測試需要資料，請使用虛構且通用的範例。

## 版本政策

目前公開版固定為 **V0.1**，套件及安裝包版本固定為 `0.1.0`。除非維護者明確決定改版，請勿自行遞增 `package.json`、lockfiles、Cargo 或 Tauri 的版本。

介面標示由 `src/version.ts` 提供，視窗與網頁標題由對應設定檔提供；`src/version.test.ts` 會檢查它們一致。資料格式的 `STATE_VERSION` 獨立管理，不能因應用程式版本重整而倒退或重設。

發布時沿用 `v0.1` 入口，於 Release 說明記錄實際建置的 Git commit、驗證結果與資產 SHA-256，避免同版更新難以辨識。重新發布及替換資產須經維護者授權。

## 程式風格

- 優先維持既有 TypeScript 型別與元件結構。
- 介面文字應清楚、簡短，並兼顧鍵盤操作與可讀性。
- 功能變更應保持本機優先，若新增外部服務須清楚揭露傳輸資料與設定方式。
